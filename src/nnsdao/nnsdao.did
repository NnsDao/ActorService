type Result = variant { Ok : int32; Err : text };
type StackingItem = record {
  "principal" : opt principal;
  duration : nat8;
  amount : nat64;
};
service : (principal) -> {
  stacking_list : () -> (vec record { nat64; StackingItem }) query;
  test : () -> (Result) query;
}